# Frontend Document

*(No frontend plans yet. This document is reserved for future frontend architecture, UI flows, and implementation notes. When adding a UI later, align with the PRD out-of-scope list, [backend.md](backend.md) REST/WebSocket contracts (no `path` on chat—same fields as `ChatRequest` in `api/rest.py`), and [user-flow.md](../00_overview/user-flow.md).)*
